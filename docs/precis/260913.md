# Precis — 2026-09-13

## Key points from the day

### `ObjectPropertyAssembler`/`PlayerPropertyAssembler` confirmed string-only; their `UIRegistry`/
`UIEntry` baseline crossings retired

Verified the two assemblers now store only the raw `bindui` name+tag string (`ObjectProperty.UIBinding`
and `PlayerProperty.BindUI` both carry a `String`, not a resolved `UIEntry`) - the flattening
`docs/Chapter_3_UI_Roadmap.md` had parked and last precis's `BACKEND_BASELINE` entry flagged as still open. With no
resolution happening at assemble time, neither assembler makes a qualified reference to
`frontend.entries.UIEntry` or `frontend.ui.globals.UIRegistry` any more, so their four stale
`BACKEND_BASELINE` lines in `BoundaryTest` were deleted.

### Two reader tests rewritten off the old registry-resolving contract

Deleting those baseline lines surfaced `ObjectPropertyReaderTest` and `PlayerPropertyReaderTest` still compiled against
the old contract - seeding a real `UIRegistry`, asserting a resolved `UIEntry`, and expecting an "illegal UIEntry" soft
error for an unmatched `bindui` target. Rewrote both: dropped the registry-seeding scaffolding, switched assertions to
plain string equality, and replaced the now-impossible "illegal UIEntry" case with a test confirming a `bindui` naming a
non-existent entry is captured verbatim without error - resolution is deferred entirely to the UI layer now.

### `MIDDLE_BASELINE`'s two `UIEntry` crossings for `ObjectProperty`/`PlayerProperty` cleared

After removing the now-dead `UIEntry` import from `ObjectProperty.java`, and then the same Javadoc-only reference from
`PlayerProperty.java`, neither file makes a qualified reference to
`frontend` any more, so both `MIDDLE_BASELINE` lines came out too, re-read fresh from each file before each removal.
`MIDDLE_BASELINE` is now down to the three pre-existing monster colour-cycle crossings. Full suite reconfirmed clean
after every one of the four edits: 4930 tests, 0 failures, 0 errors.

---

## Things not to drop

- The raw entry names now sitting in `ObjectProperty`/`PlayerProperty` aren't resolved anywhere yet - no
  `UIRegistryLoader` class exists in `src/main` today. That lookup is the actual Chapter 3 UI work; today's change only
  cleared the backend/middle side of the crossing.
