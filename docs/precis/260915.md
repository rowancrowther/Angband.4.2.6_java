# Precis — 2026-09-15

## Key points from the day

### `SavefileGetterImpl.gotSavefile` verified, fixed, and covered

`SavefileGetterImpl` - the Java port of C's `struct savefile_getter_impl` and `got_savefile()` (`src/ui-game.c`) - was
checked against the original this session. Two real discrepancies turned up in the existing port: the directory-open
success flag was set unconditionally instead of reflecting whether the save directory actually opened, and the
null-directory guard was conflating "never opened" with "enumeration already finished", logging an error on what is an
entirely ordinary case for a caller. You fixed both, adding a flag that tells the two null-directory causes apart the
same way the C original's two separate call branches do. The class and its methods now carry full Javadoc with the C
cross-reference, and a new test class covers a directory that fails to open, an empty directory, several entries
enumerated one at a time, and sub-directories being skipped; the full project suite is green.

### `SavefileDetails` verified and covered

`SavefileDetails` - the Java port of C's `struct savefile_details` (`src/ui-game.h`) - was checked against the original
this session. The C struct is a plain data holder with no behaviour of its own, populated field-by-field by
`got_savefile()` in `src/ui-game.c`, and the Java class already mirrored that shape exactly: `fileName`/`description`/
`offset` line up one-to-one with `fnam`/
`desc`/`foff`, with empty strings standing in for the C struct's zero-allocated `NULL`s. No divergence was found, so the
pass moved straight to documentation and tests. The class and all six members now carry full Javadoc with the C
cross-reference, and `SavefileDetailsTest` covers the default zero-allocated state plus a round trip through every
setter/getter pair, including the
`foff`-as-prefix-length case the C comment calls out; the full project suite is green. The reference C source's stamping
step was blocked by a permission denial again this session, the same snag as 260914's `AngFile` pass, and still needs
doing by hand or with permissions adjusted.

### `GameConstantsParseResult` verified and documented

`GameConstantsParseResult` - the holder `GameConstantsReader.parseWithResults` returns, pairing the assembled game
constants with any parse errors - was checked this session. It has no single C counterpart to diverge from, so the pass
confirmed it faithfully carries the two outcomes the reader already produces: a populated result, or a null-data result
with the errors collected during assembly; no discrepancy turned up. Its Javadoc now carries the same dated provenance
stamps as the rest of today's Reader sweep, and a small dedicated test class pins its two-state contract independently
of the ANTLR reader pipeline. This closes out the one file the Reader Javadoc sweep had missed, leaving the package
internally consistent.

### `ColourTranslation` verified

`ColourTranslation` - the Java port of C's nine `ATTR_*` display-mode indices (`src/z-color.h`) - was checked against
the original this session. All nine constants line up value-for-value with their C counterparts, and the class already
carried full Javadoc in the house style, so no `src/main` edit was needed. `ColourEnumTest`'s existing
`theTranslationIndicesAreTheNineTableColumnsInOrder` case already pins every one of the nine values against the C
source, so no separate test file was added; the full project suite is green. This closes out the enum as fully ported
alongside the `ColourEnum` work that already depended on it.

### `EDTSender` verified and documented

`EDTSender` - the EDT's send-only view onto the UI thread's inbox, part of the two-channel CSP migration - was checked
this session. It has no single C counterpart to diverge from, so the pass confirmed it against the migration's own
design instead: it only ever offers to the shared, unbounded queue and never blocks, matching what `Channels.create()`
wires it onto and what `Sender`'s contract promises; no discrepancy turned up. Its Javadoc now carries the same dated
provenance stamps as the rest of the sweep, and its behaviour - delivery to the UI thread, exclusion from the core,
shared ordering with the core sender, and never blocking a reader-less queue - is already pinned by `ChannelsTest`'s
`EdtView` nest, so no new test file duplicates that coverage. This leaves the EDT's end of the channel package fully
documented ahead of stage 5's boundary test.

### `CoreReceiver` verified and documented

`CoreReceiver` - the core's own reading end of its inbox, part of the two-channel CSP migration - was checked this
session. It has no single C counterpart to diverge from, so the pass confirmed it against the migration's own design
instead: it blocks on the shared queue exactly as `Receiver`'s contract promises, and its narrow `UIMessage` typing
matches what `Channels.create()` wires it onto; no discrepancy turned up. Its Javadoc now carries the same dated
provenance stamps as the rest of the sweep. Its behaviour - delivery from the UI sender, blocking with nothing queued,
and waking on interruption - is already pinned by `ChannelsTest`'s `Wiring` and `AcrossTwoThreads` nests, so no new test
file duplicates that coverage. This leaves the core's end of the channel package fully documented alongside `EDTSender`,
ahead of stage 5's boundary test.

### `CoreSender` verified and documented

`CoreSender` - the core's writing end of the UI thread's inbox, part of the two-channel CSP migration - was checked this
session. It has no single C counterpart to diverge from, so the pass confirmed it against the migration's own design
instead: it only ever offers to the shared, unbounded queue and never blocks, and its wide-field/narrow-method typing
(`ChannelMessage` on the queue, `CoreMessage` on `send`) matches what `Channels.create()` wires it onto and what
`Sender`'s contract promises; no discrepancy turned up. Its Javadoc now carries the same dated provenance stamps as the
rest of the sweep. Its behaviour - delivery to the UI receiver, exclusion from the core's own receiver, shared ordering
with the EDT sender, and never blocking a reader-less queue - is already pinned by `ChannelsTest`'s `Wiring` and
`Delivery` nests, so no new test file duplicates that coverage. This closes out the channel package's three sender/
receiver views alongside `EDTSender` and `CoreReceiver`, ahead of stage 5's boundary test.

---

## Things not to drop

- `SavefileGetterImpl.gotSavefile` has no equivalent of C's SETGID-prefix filtering (`ui-game.c:1204-1209` sets up
  `uid_c`, `ui-game.c:1231-1234` skips any `fname` that doesn't start with it) - not a bug, since SETGID doesn't apply
  to this port, but the "only show this player's own savefiles" behaviour it provided has no Java replacement yet if a
  shared/multi-user save directory is still wanted.
- `ChestTrapAssembler.assemble` is all-or-nothing, unlike every sibling assembler in the parser package - one bad record
  zeroes the entire chest-trap list rather than dropping just that record
  (`backend/parser/chesttrap/ChestTrapAssembler.java`).
- `ObjectPropertyAssembler.java:212` and `PlayerPropertyAssembler.java:130-135` both carry dead, commented-out
  `UIRegistry.getUIEntry(...)` lookups - in both cases a binding whose UI-entry currently never gets resolved or
  validated.
- `ObjectBaseAssembler.assemble` appends a synthetic `TV_NONE` sentinel `ObjectBase` after the loop, unconditionally,
  regardless of what was actually parsed from the file.
- `PlayerRaceAssembler.java:150` always looks up player body index 0 (`PlayerRegistry.lookupPlayerBody(0)`) regardless
  of which race is being assembled.
- `ProjectionAssembler.assemble` silently falls back to sentinel `-1` on a parse failure for `numerator`/`divisor`/
  `damageCap`, with no error reported - unlike every other numeric field in the same method, which reports and skips.
- `VaultAssembler`'s `logger` field is declared but never invoked anywhere in the class.
- `TrapAssembler.assemble` computes `trapIndexStr` from `record.index()` but never uses it - the real index is the loop
  counter.
