# Precis — 2026-09-16

## Key points from the day

### `BitwiseOrCombiner` verified and documented

`BitwiseOrCombiner` - the `BITWISE_OR` row of the character-sheet value combiner table, folding contributions with a
bitwise union of their bits - was checked against C's `simple_combine_init`, `bitwise_or_combine_accum_help`/
`bitwise_or_combine_accum`, `dummy_combine_finish`, and `bitwise_or_vec` (`ui-entry-combiner.c`) this session. Every
clause matched: the sentinel-handling order in the accumulator helper, the plain `|=` union with no saturation clamp
(unlike `ADD`'s), and the `n`-bounded loop in the one-shot form; no discrepancy turned up. The class already carried
thorough prose, but its `state` field had no Javadoc at all and none of its blocks carried the house-style provenance
stamp, so this pass added the field's doc and stamped every existing block instead of leaving them half-documented.
`CombinerTest`'s existing `BitwiseOr` nest already exercises every branch found in verification, so no new test file was
needed; the full project suite is green. This is the second of the nine `combiners` implementations to carry the
provenance stamp, alongside `AddCombiner`, `Combiner`, and `UIEntryCombinerState`.

### `FirstCombiner` verified and documented

`FirstCombiner` - the `FIRST` row of the character-sheet value combiner table, keeping only the opening contribution and
discarding every later one - was checked against C's `simple_combine_init`, `dummy_combine_accum`,
`dummy_combine_finish`, and `first_vec` (`ui-entry-combiner.c`) this session. Every clause matched: the seed-only
`init`, the no-op `accum` that leaves later contributions untouched, the pass-through `finish`, and the `n`-bounded
one-shot form that reads only element zero; no discrepancy turned up. The class already carried thorough prose, but its
`state` field had no Javadoc at all and none of its blocks carried the house-style provenance stamp, so this pass added
the field's doc and stamped every existing block instead of leaving them half-documented. `CombinerTest`'s existing
`Positional` nest already exercises both the incremental and one-shot paths, so no new test file was needed; the full
project suite is green. This is the third of the nine `combiners` implementations to carry the provenance stamp,
alongside `AddCombiner`, `BitwiseOrCombiner`, `Combiner`, and `UIEntryCombinerState`.

### `LargestCombiner` verified, documented, and given fuller branch coverage

`LargestCombiner` - the `LARGEST` row of the character-sheet value combiner table, folding contributions down to the
greatest value seen on each channel - was checked against C's `simple_combine_init`, `largest_combine_accum_help`/
`largest_combine_accum`, `dummy_combine_finish`, and `largest_vec` (`ui-entry-combiner.c`) this session. Every clause
matched: the sentinel-handling order in the accumulator helper, the direct (non-helper) seeding of both `init` and the
one-shot form's first element, and the `n`-bounded loop; no discrepancy turned up. The class already carried thorough
prose, but its `state` field had no Javadoc at all and none of its blocks carried the house-style provenance stamp, so
this pass added the field's doc and stamped every existing block; `CombinerTest`'s `Extremes` nest was missing coverage
for three of the helper's branches (an absent accumulator meeting `UNKNOWN`, an `UNKNOWN` accumulator meeting a real
value, and an absent accumulator meeting a real value), so three new tests were added there rather than left as gaps.
The full project suite is green. This is the fourth of the nine `combiners` implementations to carry the provenance
stamp, alongside `AddCombiner`, `BitwiseOrCombiner`, `FirstCombiner`, `Combiner`, and `UIEntryCombinerState`.

### `LogicalOrCombiner` verified, documented, and given fuller branch coverage

`LogicalOrCombiner` - the `LOGICAL_OR` row of the character-sheet value combiner table, answering "does anything grant
this at all?" by flattening every real contribution to `0`/`1` before OR-ing - was checked against C's
`logical_combine_init`, `logical_or_combine_accum_help`/`logical_or_combine_accum`, `dummy_combine_finish`, and
`logical_or_vec` (`ui-entry-combiner.c`) this session. Every clause matched: the flattening in `init`, the
sentinel-handling order in the accumulator helper, and the one-shot form's from-index-zero fold seeded at
`NOT_PRESENT` rather than from element zero (the one point where this row's `vec` shape differs from the arithmetic
rows); no discrepancy turned up. The class already carried thorough prose, but its `state` field had no Javadoc at all
and none of its blocks carried the house-style provenance stamp, so this pass added the field's doc and stamped every
existing block; `CombinerTest`'s `LogicalOr` nest was missing coverage for the helper's `UNKNOWN`-related branches (an
`UNKNOWN` contribution alone, filling an absent accumulator, replacing an `UNKNOWN` accumulator, and failing to disturb
an already-set one), so four new tests were added there rather than left as gaps. The full project suite is green. This
is the fifth of the nine `combiners` implementations to carry the provenance stamp, alongside `AddCombiner`,
`BitwiseOrCombiner`, `FirstCombiner`, `LargestCombiner`, `Combiner`, and `UIEntryCombinerState`.

### `SwingUI.JPanelArea.put(char)` and `put(String)` verified and documented, with two bugs found and fixed

`JPanelArea`'s two `put` overloads - the front end's port of C's `Term_putch` and `Term_putstr` - were checked against
`Term_gotoxy`, `Term_addch` and `Term_addstr` (`ui-term.c`) this session. The string form's handling of a negative
starting column disagreed with C - it drew the string's trailing characters instead of declining the whole write, which
is what a failed `Term_gotoxy` does - and the panel's font was being silently overwritten by the constructor after
`init()` had already installed the correctly-measured one; both were fixed in the working tree, and the char form's
Javadoc, which wrongly claimed an out-of-range write throws, was rewritten to describe the actual silent-decline
behaviour instead. The `logger` and `screen` fields, previously undocumented, also got Javadoc naming what actually
reads them. Nine new tests cover both overloads' bounds and clipping behaviour at every edge `Term_gotoxy` checks,
derived from the C source rather than the Java code, and the full project suite is green - closing out the last of
`SwingUI.java`'s missing field Javadoc from `docs/no_low_javadoc.md`.

### `Colour` verified and documented

`Colour`, the runtime palette holder behind every character drawn on screen, was checked against C's
`angband_color_table` and its sizing constants `MAX_COLORS`/`BASIC_COLORS` (`z-color.c`, `z-color.h`) this session. All
29 named RGB triples matched their C rows exactly, as did `maxColours` and `basicColours`; no discrepancy turned up. The
class carried a decent class comment but was missing Javadoc on the `colourTable` field and on `getColour`
entirely, and no block carried the house-style provenance stamp, so this pass filled both gaps and noted, as a design
aside, that C keeps no separate copy of its compiled-in defaults the way `originalColours` does here. A new
`ColourTest` pins every basic colour's RGB value against the C source directly, plus the first/last-row boundaries and
re-`init()` safety, and the full project suite is green - this is the first Javadoc pass over the `colour` package.

### `MonsterRaceCycler` fixed, documented, and tested

`MonsterRaceCycler`, the per-race colour-cycle lookup that animated monsters check before falling back to `FlickerTable`
and then their static colour, was checked against C's `visuals_color_cycles_by_race` table and its
`visuals_cycler_set_cycle_for_race`/`visuals_cycler_get_attr_for_race` accessors this session. Three rounds of
verification each turned up a real mismatch - a value type that stored a whole cycle table per race instead of one
resolved cycle, an uncallable frame-lookup method, and a fix for that which reset the shared cache on every call - each
reported and fixed in turn until the fourth pass matched C's behaviour clause for clause, including its null guards and
its "unresolved cycle stores nothing" rule. The class, its field, and every method now carry full Javadoc naming the C
cross-reference and the deliberate divergences (name-keyed `HashMap` instead of C's manually-grown `ridx` array, `null`
instead of the `BASIC_COLORS` sentinel). A new `MonsterRaceCyclerTest` covers the ordinary wraparound path, the
single-step boundary, both null guards, re-registration, and the raw accessor, all derived from the C source; the full
project suite is green - this is the first test coverage for the `colour` package's cycling primitives.

### `ColourCycle` verified and documented

`ColourCycle`, the single frame-list primitive shared by both `VisualsCycler`'s named cycles and `FlickerTable`'s
base-attribute entries, was checked against C's `visuals_color_cycle` struct and its `_new`/`_attr_for_frame`
functions (`ui-visuals.c`) this session. Every clause matched, including the already-documented deliberate divergence:
C fixed-width-allocates `VISUALS_STEPS_MAX` slots per cycle with an invalid-colour sentinel and wraps `frame %
max_steps` internally, while this port stores only the declared steps and leaves the wrap to the caller, exactly as
`MonsterRaceCycler` already does; no discrepancy turned up. The class and its two fields already carried thorough prose
but no house-style provenance stamp, and the constructor, factory, and both getters had no Javadoc at all, so this pass
stamped the existing blocks and wrote full documentation for the rest, naming each one's C cross-reference. A new
`ColourCycleTest` covers the empty-list refusal, exact-size building, the single-step boundary, the out-of-bounds throw
where C would fall back to a sentinel, and the defensive copy against a mutated source list; the full project suite is
green.

### `UIEntry` verified, documented, and tested

`UIEntry`, the single-line data holder behind each stat/resistance row on the player status display, was checked against
C's `struct ui_entry` and its `ui_entry.txt` parser (`ui-entry.c`) this session. The category search matched C's binary
search in effect - a plain linear scan needs no sortedness invariant to find the same match - but the constructor kept a
`description` field the C struct never carries, since C validates and discards the `desc:` field at parse time rather
than storing it. The field was removed, keeping the constructor's null check as deliberate input validation on data the
class doesn't retain, and every declaration in the file - the class, its fields, the constructor, both category-search
methods, the `StatElemType` enum and its `fromValue` lookup, and every getter - got full Javadoc with the house-style
provenance stamp. A new `UIEntryTest` covers the description validation, category search hits and misses, the flag test,
and `fromValue`'s three resolvable names plus its unknown-name fallback, all derived from the C source; the full project
suite is green.

### `UIEntryBase` verified, fixed, documented, and tested

`UIEntryBase`, the template half of the UI-entry system letting several status-display entries share a renderer,
combiner, category list, and flag set, was checked against `struct ui_entry` and its `ui_entry_base.txt` parser this
session, alongside `UIEntry`'s sibling pass earlier today. The first verify pass found the same
description-not-discarded mismatch as `UIEntry`, plus a raw, unvalidated flags string standing in for C's resolved
bitmask; you fixed both, but the flags fix introduced a regression - splitting on the regex `"|"` instead of a literal
pipe tore every real flag value into single characters, so the one flag the shipped data actually uses would have failed
to parse - alongside a compile-breaking return-type mismatch on the flags getter. Both were caught on the next verify
pass and fixed before Javadoc began. Every declaration in the file now carries full Javadoc with the provenance stamp,
and a new
`UIEntryBaseTest` covers the description validation and the flag parsing boundary (single flag, pipe-separated multiple
flags, and rejection of an unknown token) that the regression would have broken; a stale existing test that still
asserted the old raw-string flags and dropped description was brought back to green alongside it, and the full project
suite is green. A case-insensitive flag match here against C's case-sensitive one was noted in chat as an unexercised
divergence rather than fixed, since no shipped data currently triggers it.

### `UIEntryIterator` fixed, documented, and tested

`UIEntryIterator`, the cursor over a filtered, priority-sorted run of UI entries that `UIPlayer` draws its stat-modifier
list from, was checked against C's `struct ui_entry_iterator` and its `advance_`/`count_ui_entry_iterator`
functions (`ui-entry.c`) this session. Two rounds of verification each turned up a real mismatch: `advance()`
incremented its cursor before reading instead of after, silently dropping the first matching entry and nulling out the
last slot in `UIPlayer`'s stat-modifier loop, and the first fix for `getNum()`'s remaining-count formula then
overcorrected by one. Both were reported and fixed in turn until the third pass matched C's read-then-increment and
`n - i` behaviour clause for clause. Every declaration in the file now carries full Javadoc naming the C
cross-reference, and a new `UIEntryIteratorTest` covers ordinary iteration order, the remaining-count boundary down to
zero, the safety-net null past exhaustion, the three-argument constructor's arbitrary starting index, and the build-up
path
`UIEntryCode` uses, all derived from the C source; the full project suite is green.

### `UIEntryRenderer` verified and documented

`UIEntryRenderer`, the concrete named renderer instance backing each UI status entry's colour/symbol/digit/sign choices,
was checked against C's finished `struct renderer_info` (`ui-entry-renderers.c`) this session. The class carries no
logic of its own - it only stores what it's constructed with - so there was nothing for behaviour to diverge on; the
class doc and all seven fields already had prose but no house-style provenance stamp, and six of the seven getters had
no Javadoc at all. This pass stamped every existing block and wrote full documentation for the missing getters, naming
each field's C cross-reference, and a new `UIEntryRendererTest` pins the constructor/getter round trip against two of
the C table's default renderer rows - including the zero-digit case - rather than the Java enum, so the full project
suite is green. A genuine mismatch was noticed in passing in `UIEntryRendererAssembler`, a different file outside this
pass's scope, where a partial colour/symbol override replaces the whole default table instead of padding it the way C's
`augment_colors`/`augment_symbols` do - worth a separate look.

### `UIEntryRendererEnum` verified and documented

`UIEntryRendererEnum`, the six built-in renderer descriptors a data-file renderer falls back on for any field it leaves
unset, was checked against the `UI_ENTRY_RENDERER` rows of C's `list-ui-entry-renderers.h` this session, alongside
`UIEntryRenderer`'s sibling pass earlier today. Every combiner name, packed colour/label/symbol table, digit count, and
sign mode matched its C row character for character, and the enum's extra `UI_ENTRY_RENDERER_NONE` constant checked out
as a sensible Java-only sentinel for the index-0 "invalid renderer" gap C's own indexing convention leaves open; no
discrepancy turned up. The class and all six fields already carried prose but no house-style provenance stamp, so this
pass stamped every existing block and tightened the four getters' generic descriptions to name their C cross-reference,
and a new `UIEntryRendererEnumTest` pins every constant's table values against `list-ui-entry-renderers.h` directly
rather than the Java source, so the full project suite is green. The enum still has no getter for its `combiner` field,
which left that column of the C table unverifiable by test - worth a separate look if that accessor is ever added.

### `UIEntryCategory` verified and documented

`UIEntryCategory`, the resolved (name, priority, priority-set) triple attached to each `UIEntry`, was checked against
C's `struct category_reference` (`ui-entry.c`) this session. The class carries no logic of its own - a constructor
assigning three fields and three plain getters - so there was nothing for behaviour to diverge on; C's own resolution
step, where an unset priority is zeroed and flagged false while a set one is copied across verbatim, already happens
before this struct's fields are filled, not inside it. The class and all three fields had no Javadoc at all, so this
pass wrote full documentation naming the C cross-reference, including the split between this resolved struct and C's
separate `embryonic_category_reference` used mid-parse. A new `UIEntryCategoryTest` covers the explicit-priority and
unset-priority construction paths, a negative priority from C's negative-index scheme, and an explicit zero priority
staying flagged as set, all derived from the C source; the full project suite is green.

### `UILoop` verified, documented, and tested

`UILoop`, the UI half's message loop that turns the core's channel messages into painting calls and the shutdown
handshake, was checked against C's `game-event.c` dispatch and the `ui-display.c` handlers it replaces
(`init_angband_aux`, `show_splashscreen`, `ui_enter_init`) this session. Two of the C-derived string literals in the
"lib directory is broken" report had drifted from C's exact wording - a missing quote-and-exclamation pair in the
missing-news.txt message, and a blank line that took three rounds to land in the right place inside the four-line report
itself - both reported and fixed in turn before the class matched clause for clause. Every declaration in the file now
carries full Javadoc with the provenance stamp, including a previously-undocumented field and two
previously-undocumented methods, and two new tests pin both fixed strings against the C source directly rather than the
Java code; the full project suite is green, and the loop's dispatch and shutdown-handshake behaviour keeps the thorough
coverage an earlier pass already gave it.

### `Term.cPrt`'s `termErase` helper fixed and documented

`Term.cPrt`'s private `termErase` helper - the boundary behind `cPrt`/`prt`'s clear-then-write behaviour - had been
flagged earlier today for blanking a row even when the requested column was out of range, unlike C's `Term_erase`, which
returns immediately the moment `Term_gotoxy` rejects the coordinate. You added the missing guard so
`termErase` now leaves the row untouched on a failed move, matching C exactly; `TermPrtTest`'s previously-failing
`aNegativeColumnWritesAtTheStaleCursorPositionInstead` now passes, and the rest of the `Term` test package stays green
alongside it. `cPrt`'s own Javadoc, which had drifted to describe a delegation the code no longer used, was rewritten to
match, and `termErase` and the previously-undocumented `outputHook` field both got full documentation for the first
time. This closes the open item logged earlier today and leaves `cPrt`/`prt`'s coordinate handling matching C for every
boundary case the test file exercises.

### `TermData.termDataLink` verified, fixed, documented, and tested

`TermData.termDataLink`, the per-window setup that binds a logical `Term` to its platform window and wires up its
drawing hooks, was checked against C's `term_data_link` this session - the Windows front end's own per-window link step.
The double-height hook came out wrong: Java was wiring the shared drawing hook onto it, where C deliberately leaves it
null at link time and only installs one later, conditionally, elsewhere in the same file - a live control-flow gate
downstream, so the divergence was real rather than cosmetic, and was reported before anything else in the file was
touched. You fixed the hook assignment in the working tree, and the class, its two previously-undocumented fields, and
the method itself now all carry full Javadoc naming the C cross-reference and the house-style provenance stamp. A new
run of `TermDataTest` cases pins the fixed boundary alongside the flag and hook wiring the method sets up, covering both
the given-a-term and self-built-`Term` branches, and the full project suite is green.

### `TermWin` bug found and fixed, then documented and tested

`TermWin`, the per-terminal content buffer backing `Term`'s `old`/`scr` window pair, was checked against C's `term_win`
struct and `term_win_init` (`ui-term.c`) this session. `init`'s grid allocation had
`width`/`height` transposed against the fill loop's indexing, which only happened to work for a square terminal and
threw `ArrayIndexOutOfBoundsException` for a real one like Angband's default 80x24; you fixed the transposition and
swapped the initial fill from space-and-white to the NUL-and-`COLOUR_DARK` placeholder C's zeroed allocation actually
produces. Once verification came back clean, every declaration in the file got full documentation, including a
correction to the class and field Javadoc's description of `a`/`va` as an in-place working/displayed pair - that diffing
actually happens between `Term`'s `old` and `scr`
instances, not within one `TermWin` - and a stale `z-term.h` cross-reference across six methods was corrected to
`ui-term.h`. A new `TermWinInitTest` covers both non-square dimension directions, the previously-buggy shape
specifically, the smallest legal window, and the zeroed-cell fill, alongside the existing cursor getter/setter coverage;
the full project suite is green. This closes out `TermWin`'s only remaining undocumented method and removes a live crash
from the terminal-buffer boundary before anything downstream starts writing through it.

### `TextOut.nextSection` bug found and fixed, then verified and tested

`TextOut.nextSection`, the tag-splitting scan behind `textOutE`'s coloured-section output, was checked against C's
`next_section` (`z-textblock.c`) this session. The invalid-tag branch resumed its next `{` search from wherever the
letter scan had stopped instead of from just past the discarded tag's own opening brace, so an invalid tag whose body
consumed letters before running into a second `{` skipped straight past that second brace and lost a real tag entirely

- reported and left for you to fix rather than edited here. You corrected the resume point to the opening brace's own
  position, and a re-verify pass confirmed every clause now matches C exactly, including that failing case traced by
  hand both before and after the fix. Javadoc on the method was already accurate to the intended behaviour and needed no
  changes; a new `TextOutNextSectionTest` case pins the letters-then-brace scenario that the bug broke, and the full
  project suite is green.

### `Window.show` documented and tested

`Window.show`, the call that pushes a painted `Frame` onto a window's grid and repaints it, was checked this session
against the C original the `port` process looks for - and there isn't one. `Frame` is documented new architecture with
no C counterpart, so `show` is the Java side of a boundary the C front end never had; the closest tie is the conceptual
one `UILoop` already names in its own comments, that this method plays the role C's `Term_fresh()` flush plays at the
end of `splashscreen_note()`. It was the one declaration left undocumented in an otherwise fully-Javadocked file, so
this pass wrote its block, naming the event-dispatch-thread hop and the row-sharing contract `CellGrid.getCells()`
already documents for this method as its one caller. A new `WindowShowTest` covers the ordinary paint-through case and
pins that row-sharing contract directly, by reflection, and the full project suite is green.

### `CombinerName` verified and documented

`CombinerName`, the name-indexed enum a UI entry binds its combining algorithm through, was checked against C's
alphabetically-sorted `combiners[]` table and its `ui_entry_combiner_lookup`/`ui_entry_combiner_get_funcs` pair
(`ui-entry-combiner.c`/`.h`) this session, since there is no single C function it ports one-to-one. Every named constant
resolved to the right table row in the right order, and the Java-only `NONE` constant's `null` prototype turned out to
mirror the C side exactly: the C original never calls a combining function on a zero `combiner_index`, guarding that
precondition with an outright `assert(0)` at one call site, so a `NullPointerException` from calling `init` on `NONE` is
the same "must not happen" contract enforced the Java way rather than a divergence; no discrepancy turned up. The class,
its `combiner` field, the constructor, and `init` had either no Javadoc or bare one-liners with no C cross-reference, so
this pass wrote full documentation for all of them plus every enum constant, naming each one's row in the C table. A new
`CombinerNameTest` pins `init`'s seeded value against the C `*_init`/`*_finish` pairs directly for every strategy
(including the two, `LOGICAL_OR_WITH_CANCEL` and `RESIST_0`, whose seeding isn't a plain pass-through), the `NONE`
precondition, and the clone-per-call isolation that is this file's whole reason for existing; the full project suite is
green.

### `Screen.splashScreenNote` verified and documented

`Screen.splashScreenNote`, the boundary method that writes the load-progress note onto row 23 while game data loads, was
checked against the non-birth branch of C's `splashscreen_note` (`ui-display.c`) this session. Every clause matched -
the bracketing, the row-23 erase, and the 80-column centring - and the method's missing `MSG_BIRTH` handling turned out
to already be a documented gap in `InitHandlers`, not a new discrepancy. The method and its previously-undocumented
`frame` field, the last two undocumented declarations in `Screen`, now carry full Javadoc naming the C cross-reference
and the birth-note gap. A new suite of `ScreenTest` cases pins the bracketing, centring (including its rounding and an
over-wide message's clip), and row-23 erase against the C source, and the full project suite is green.

### `TermTextHook`/`TermScreenHook` trimmed to match C's real hook table, then documented and tested

`TermTextHook` and `TermScreenHook` were checked against C's actual `struct term` hook table (`ui-term.h`) rather than a
single ported function, since the interface's own shape was in question. Their `cPrt` method had no counterpart in that
table and was never called - `Term.cPrt` already built its behaviour from `termErase` and `addstr`, the two calls that
do reach the real boundary - so the interface had drifted to describe a hook that didn't exist. You removed `cPrt` from
both files, leaving `putStr` and `erase` as the only members, matching C's `text_hook`/`wipe_hook` pair exactly. Every
declaration in both files now carries full Javadoc naming the C cross-reference, and a new
`TermScreenHookTest` exercises the hook directly - the negative-`n` "unlimited" branch and the
`col`/`row` parameter swap in `erase`, neither reachable through any existing `Term`-level test - with the full project
suite green. This leaves the hook boundary matching C's own driver-table shape, closing off the drift risk of a second,
unguarded `cPrt` implementation.

### `TermXtraWinDelay` fixed, documented, and tested

`TermXtraWinDelay`, the dedicated Windows delay-only hook implementation of `TermEventHook`, was checked against C's
`Term_xtra_win_delay` (`main-win.c`) this session, starting from an empty stub `doSomething` body. Two rounds of
verification each turned up a real mismatch before the guard landed correctly: the first pass slept unconditionally with
no check against a negative value, which would have thrown `IllegalArgumentException` out of `Thread.sleep` rather than
the safe no-op C's own `v > 0` guard gives a non-positive delay, and swallowed any `InterruptedException` instead of
restoring the thread's interrupt status. Both were reported and fixed in turn until the guard and interrupt-status
restore matched C's behaviour clause for clause. The class and its method now carry full Javadoc naming the C
cross-reference and the Java-only interrupt-restore behaviour C has no counterpart for, and a new
`TermXtraWinDelayTest` covers the positive, zero, and negative delay paths plus the restored interrupt status on an
interrupted sleep, all derived from the C source; the full project suite is green.

### `TermXtraWin.termXtraWinNoise` and `MessageBoxFlags` fixed, documented, and tested

`TermXtraWin`'s noise handler and its `MessageBoxFlags` enum - the boundary behind C's `Term_xtra_win_noise`, which
always fires `MessageBeep(MB_ICONASTERISK)` - were checked this session. The handler fetched the sound file associated
with the flag but never played it, so the game's noise cue was a silent no-op; once real playback was wired in, the
referenced sound asset itself turned out to be missing from the checkout, so nothing existed at the path
`MessageBoxFlags` pointed at either. Both were reported in turn and fixed in the working tree - a
`javax.sound.sampled` `Clip` open/start added to the handler, and a real `message.wav` added under `lib/sounds/` - until
a scratch run of the dispatch path completed clean with no swallowed exception. Both files now carry full Javadoc naming
the C cross-reference and the house-style provenance stamp, and new `MessageBoxFlagsTest`/
`TermXtraWinTest` suites cover the only-`MB_ICONASTERISK`-carries-a-sound rule, the asset's presence and readability,
and the noise dispatch never throwing regardless of audio availability; the full project suite is green.

---

## Things not to drop
