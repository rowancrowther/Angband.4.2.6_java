# Precis — 2026-09-17

## Key points from the day

### `SplashScreen` verified and its test suite fixed to green

`SplashScreen`'s `readAndParse`, `splashScreenNote`, and `splashScreenBirthNote` - the port of
`show_splashscreen`'s parsing half and both branches of `splashscreen_note` (`ui-display.c`) - already carried full
Javadoc and a `SplashScreenTest` suite derived from the C source from an earlier session, so this pass picked up from
unit tests onward. Two of the fourteen birth-note tests threw `NullPointerException`, since they never built the
`JPanelArea` their sibling test constructs by hand, and `Window.getArea()` returns nothing until a panel has been added.
Adding that same setup call to both tests was the only change needed, and the full project suite is green.
`show_splashscreen` gets its C-side "Ported to Java" stamp with this pass, alongside the already-stamped
`splashscreen_note`, closing out this class for now.

---

## Things not to drop
