# Precis — 2026-09-15

## Key points from the day

### `SavefileGetterImpl.gotSavefile` verified, fixed, and covered

`SavefileGetterImpl` - the Java port of C's `struct savefile_getter_impl` and `got_savefile()` (`src/ui-game.c`) - was
checked against the original this session. Two real discrepancies turned up in the existing port: the directory-open
success flag was set unconditionally instead of reflecting whether the save directory actually opened, and the
null-directory guard was conflating "never opened" with "enumeration already finished", logging an error on what is an
entirely ordinary case for a caller. You fixed both, adding a flag that tells the two null-directory causes apart the
same way the C original's two separate call branches do. The class and its methods now carry full Javadoc with the C
cross-reference, and a new test class covers a directory that fails to open, an empty directory, several entries
enumerated one at a time, and sub-directories being skipped; the full project suite is green.

### `SavefileDetails` verified and covered

`SavefileDetails` - the Java port of C's `struct savefile_details` (`src/ui-game.h`) - was checked against the original
this session. The C struct is a plain data holder with no behaviour of its own, populated field-by-field by
`got_savefile()` in `src/ui-game.c`, and the Java class already mirrored that shape exactly: `fileName`/`description`/
`offset` line up one-to-one with `fnam`/
`desc`/`foff`, with empty strings standing in for the C struct's zero-allocated `NULL`s. No divergence was found, so the
pass moved straight to documentation and tests. The class and all six members now carry full Javadoc with the C
cross-reference, and `SavefileDetailsTest` covers the default zero-allocated state plus a round trip through every
setter/getter pair, including the
`foff`-as-prefix-length case the C comment calls out; the full project suite is green. The reference C source's stamping
step was blocked by a permission denial again this session, the same snag as 260914's `AngFile` pass, and still needs
doing by hand or with permissions adjusted.

### `GameConstantsParseResult` verified and documented

`GameConstantsParseResult` - the holder `GameConstantsReader.parseWithResults` returns, pairing the assembled game
constants with any parse errors - was checked this session. It has no single C counterpart to diverge from, so the pass
confirmed it faithfully carries the two outcomes the reader already produces: a populated result, or a null-data result
with the errors collected during assembly; no discrepancy turned up. Its Javadoc now carries the same dated provenance
stamps as the rest of today's Reader sweep, and a small dedicated test class pins its two-state contract independently
of the ANTLR reader pipeline. This closes out the one file the Reader Javadoc sweep had missed, leaving the package
internally consistent.

### `ColourTranslation` verified

`ColourTranslation` - the Java port of C's nine `ATTR_*` display-mode indices (`src/z-color.h`) - was checked against
the original this session. All nine constants line up value-for-value with their C counterparts, and the class already
carried full Javadoc in the house style, so no `src/main` edit was needed. `ColourEnumTest`'s existing
`theTranslationIndicesAreTheNineTableColumnsInOrder` case already pins every one of the nine values against the C
source, so no separate test file was added; the full project suite is green. This closes out the enum as fully ported
alongside the `ColourEnum` work that already depended on it.

### `EDTSender` verified and documented

`EDTSender` - the EDT's send-only view onto the UI thread's inbox, part of the two-channel CSP migration - was checked
this session. It has no single C counterpart to diverge from, so the pass confirmed it against the migration's own
design instead: it only ever offers to the shared, unbounded queue and never blocks, matching what `Channels.create()`
wires it onto and what `Sender`'s contract promises; no discrepancy turned up. Its Javadoc now carries the same dated
provenance stamps as the rest of the sweep, and its behaviour - delivery to the UI thread, exclusion from the core,
shared ordering with the core sender, and never blocking a reader-less queue - is already pinned by `ChannelsTest`'s
`EdtView` nest, so no new test file duplicates that coverage. This leaves the EDT's end of the channel package fully
documented ahead of stage 5's boundary test.

### `CoreReceiver` verified and documented

`CoreReceiver` - the core's own reading end of its inbox, part of the two-channel CSP migration - was checked this
session. It has no single C counterpart to diverge from, so the pass confirmed it against the migration's own design
instead: it blocks on the shared queue exactly as `Receiver`'s contract promises, and its narrow `UIMessage` typing
matches what `Channels.create()` wires it onto; no discrepancy turned up. Its Javadoc now carries the same dated
provenance stamps as the rest of the sweep. Its behaviour - delivery from the UI sender, blocking with nothing queued,
and waking on interruption - is already pinned by `ChannelsTest`'s `Wiring` and `AcrossTwoThreads` nests, so no new test
file duplicates that coverage. This leaves the core's end of the channel package fully documented alongside `EDTSender`,
ahead of stage 5's boundary test.

### `CoreSender` verified and documented

`CoreSender` - the core's writing end of the UI thread's inbox, part of the two-channel CSP migration - was checked this
session. It has no single C counterpart to diverge from, so the pass confirmed it against the migration's own design
instead: it only ever offers to the shared, unbounded queue and never blocks, and its wide-field/narrow-method typing
(`ChannelMessage` on the queue, `CoreMessage` on `send`) matches what `Channels.create()` wires it onto and what
`Sender`'s contract promises; no discrepancy turned up. Its Javadoc now carries the same dated provenance stamps as the
rest of the sweep. Its behaviour - delivery to the UI receiver, exclusion from the core's own receiver, shared ordering
with the EDT sender, and never blocking a reader-less queue - is already pinned by `ChannelsTest`'s `Wiring` and
`Delivery` nests, so no new test file duplicates that coverage. This closes out the channel package's three sender/
receiver views alongside `EDTSender` and `CoreReceiver`, ahead of stage 5's boundary test.

### `GameEventType` verified

`GameEventType` - the port of C's `game_event_type` (`src/game-event.h`), the vocabulary of every kind of game event the
UI/event system can broadcast - was checked against the original this session. All 65 constants line up name-for-name
and in the same declared order as the C original, and the class already carried full Javadoc in the house style, so no
`src/main` edit was needed.
`ChannelEnumsTest`'s existing `GameEvents` nest already pins the count, the opening and closing constants, the absence
of duplicates, and the handful of constants this migration depends on by name, so no separate test file was added; the
full project suite is green. The C original now carries this session's port stamp.

### `AngbandDirs` verified, fixed, and documented

`AngbandDirs` - the port of C's `ANGBAND_DIR_*` globals and the name/pointer table backing the `-d` switch - was checked
against the original this session. Two real discrepancies turned up in the default paths: the customize directory
carried a spurious extra path segment, and the archive directory was built from the plural folder name where C and the
shipped `lib/` tree both use the singular - either would have looked in the wrong place at runtime. You fixed both
defaults directly. The class and its enum now carry full Javadoc with provenance stamps, and a new test class pins every
directory's default path against the C-derived tree independently of the port's own arithmetic, alongside the
name-matching and mutation behaviour; the full project suite is green.

### `Angband` verified and documented

`Angband` - the port of C's build/version identity and player-facing copyright notice (`buildid.h`/`buildid.c`) - was
checked against the original this session. All five constants line up value-for-value with C: the name, the version
fallback, the concatenated build ID, the version alias, and the copyright text, which correctly appends one Java-only
attribution line beyond C's original; no discrepancy turned up. The class and its five fields now carry full Javadoc
with provenance stamps, and a new test class pins every constant's value against the C source directly, including the
copyright string's exact content and the appended line; the full project suite is green. This closes out one of the
smallest remaining unported globals classes.

### `UIMessage` verified, corrected, and covered

`UIMessage` - everything the UI side can say to the core and to itself, the two-channel migration's UI-to-core half -
was checked against the original this session. `LifecycleUIMessage`'s shutdown handshake and `SimpleUIMessage`'s role
as the UI-entry-loading rendezvous both matched their intended behaviour, but `WindowCloseRequested`'s Javadoc cited
the wrong C file for `quit_aux`, which actually lives in `z-util.c` and is reached through the Windows front end's
`WM_CLOSE` handler rather than `ui-init.c`. The citation is now fixed, `SimpleUIMessage` carries its own Javadoc
explaining the cross-thread rendezvous it performs with no C counterpart, and both previously-undocumented records
gained dedicated equality/field tests in `ChannelMessageTest`; the full project suite is green. The file's own class
Javadoc still documents two populations - raw input and player intent - as deferred to Chapter 5, and that boundary is
untouched by this pass.

### `EventDataBirthPoints` verified and covered

`EventDataBirthPoints` - the point-buy payload for character creation, the port of C's anonymous `birthpoints` struct
(`src/game-event.h`) - was checked against the original this session. It is a plain data holder with no behaviour of its
own, and the Java class already mirrored the C struct's shape exactly: the two point arrays become `Map<Stats, Integer>`
so a caller reads a stat by name instead of by raw index, and `remaining` carries straight across; no discrepancy was
found. The class and all its members now carry full Javadoc with the C cross-reference, and a new nested test class in
`EventDataTest` pins the constructor's argument order against the two same-typed maps, the pass-through (not
defensive-copy) getters, and the full-budget and zero-remaining boundaries drawn from `player-birth.c`; the full project
suite is green.

### `EventDataExplosion` verified and covered

`EventDataExplosion` - the blast-radius payload consumed by the display layer, the port of C's anonymous `explosion`
struct (`src/game-event.h`) - was checked against the original this session. It is a plain data holder with no behaviour
of its own, and the Java class already mirrored the C struct's shape and field order exactly, carrying the three
per-grid arrays as parallel lists instead of borrowed pointers; no discrepancy was found. The class and all its members
now carry full Javadoc with the C cross-reference, and a new nested test class in `EventDataTest` pins the constructor's
argument order against the two adjacent ints, the pass-through (not defensive-copy) getters for the three lists, and the
centre-grid-at-distance-zero case drawn from `project.c`; the full project suite is green. The reference C source's
stamping step was blocked by a permission denial again this session, the same snag as 260914's `AngFile`
pass, and still needs doing by hand or with permissions adjusted.

### `PlayerEventStatusUpdate` verified and corrected

`PlayerEventStatusUpdate` - the cached sidebar snapshot the two-channel migration hands the UI thread instead of C's
live `prt_*` redraws - was checked against the original this session. All thirty-eight positional field setters were
confirmed to thread their value into the correct constructor slot against `PlayerStatusView`'s record order, and the
static initializer's all-default seeding was confirmed field-for-field against a zero-initialised C `player` global; two
of the class's pre-existing Javadoc blocks turned out to be copy-pasted from a neighbouring setter, naming the wrong
method and describing the wrong behaviour. You fixed both blocks with their own accurate description, and a
parameterized sweep test that claimed to cover "all 35"
setters was actually only exercising 36 of the class's 38 - the two rows missing for those same mis-documented methods
are now in place, and its own stale count is corrected too. This leaves the sidebar snapshot's whole setter family both
accurately documented and provably covered, with nothing left resting on a copy-pasted description.

### `EventDataStat` verified and covered

`EventDataStat` - the current/other pair reused across several `PR_*` sidebar redraws - was checked against the original
this session. It has no single C union arm or `event_signal_*` function behind it, since C's `prt_hp`,
`prt_sp` and `prt_level` (`src/ui-display.c`) all read their pair straight off the `player` global rather than receiving
one as an argument; no discrepancy turned up against that design, which you confirmed covers `EVENT_HP`,
`EVENT_MANA` and `EVENT_PLAYERLEVEL`. The class now carries full Javadoc explaining why the second field is named
`other` rather than `max`, so no single reading of the pair gets baked into the name, and a new nested test class in
`EventDataTest` pins the constructor's argument order plus the equal, drained, and zero-current cases the three redraws
can produce; the full project suite is green. This closes out the one payload shape the redraw-drain design set piece
(`docs/implementation/260820_redraw_drain.md`) still needed before `redrawStuff` can be built against it.

### `PlayerStatusView` verified and documented

`PlayerStatusView` - the immutable record `PlayerEventStatusUpdate` caches and hands to the UI thread in place of C's
live sidebar redraws - was checked against the original this session. It is a plain thirty-nine-field data holder with
no behaviour of its own, so the pass verified each field's meaning directly against the `prt_*` family in
`src/ui-display.c` rather than trusting the sibling class's own Javadoc alone; every field lined up with its C source,
so no discrepancy turned up. The record now carries full class Javadoc plus a `@param` entry per component naming the
exact `prt_*` function (or raw `player`/`cave` field) it ports, following the same per-component style already
established on `EventDataStat`; no new test file was needed; since the record has no logic of its own beyond field
storage, its wiring is already covered end-to-end by `PlayerEventStatusUpdateTest` and
`PlayerEventStatusUpdateDefaultTest`, and the full project suite is green. This closes out the last undocumented piece
of the sidebar snapshot shape, leaving both halves of the `PlayerStatusView`/`PlayerEventStatusUpdate` pair fully
verified and documented.

### `Assembler` documented

`Assembler` - the generic parser-boundary interface that turns a `Reader`'s accumulated parse-records and any error
messages into the finished result type a caller consumes - was checked this session. It has no single C counterpart to
diverge from, being a Java-only abstraction over the parser package's several concrete assemblers, so the pass moved
straight to documentation once that was confirmed. The class and its one method now carry full Javadoc explaining the
interface's role and its type parameters, with provenance stamps. As a bare functional interface with no logic of its
own, it needed no dedicated test file, leaving it fully documented for the next assembler implementation to reference.

### `GrammarDriver` verified, fixed, and covered

`GrammarDriver` - the shared skeleton every list-shaped `lib/gamedata` reader delegates to - was checked against C's
`run_parser`/`parse_file` (`datafile.c`) this session. The overall pipeline and its two-channel error handling matched
the documented design, but `checkRecordCount`'s sentinel value collided with a legitimately parsed `-1`, silently
skipping the mismatch check the method exists to perform. You fixed it by tracking the format failure with its own flag
instead of overloading the count itself. The class, its `Extractor` interface (including a previously-undocumented
`extract` method), and both static methods now carry full Javadoc with provenance stamps, and a new test class pins
every branch of `checkRecordCount` plus `run`'s file-not-found path that the per-grammar reader suites don't otherwise
exercise; the full project suite is green.

---

## Things not to drop

- `SavefileGetterImpl.gotSavefile` has no equivalent of C's SETGID-prefix filtering (`ui-game.c:1204-1209` sets up
  `uid_c`, `ui-game.c:1231-1234` skips any `fname` that doesn't start with it) - not a bug, since SETGID doesn't apply
  to this port, but the "only show this player's own savefiles" behaviour it provided has no Java replacement yet if a
  shared/multi-user save directory is still wanted.
- `ChestTrapAssembler.assemble` is all-or-nothing, unlike every sibling assembler in the parser package - one bad record
  zeroes the entire chest-trap list rather than dropping just that record
  (`backend/parser/chesttrap/ChestTrapAssembler.java`).
- `ObjectPropertyAssembler.java:212` and `PlayerPropertyAssembler.java:130-135` both carry dead, commented-out
  `UIRegistry.getUIEntry(...)` lookups - in both cases a binding whose UI-entry currently never gets resolved or
  validated.
- `ObjectBaseAssembler.assemble` appends a synthetic `TV_NONE` sentinel `ObjectBase` after the loop, unconditionally,
  regardless of what was actually parsed from the file.
- `PlayerRaceAssembler.java:150` always looks up player body index 0 (`PlayerRegistry.lookupPlayerBody(0)`) regardless
  of which race is being assembled.
- `ProjectionAssembler.assemble` silently falls back to sentinel `-1` on a parse failure for `numerator`/`divisor`/
  `damageCap`, with no error reported - unlike every other numeric field in the same method, which reports and skips.
- `VaultAssembler`'s `logger` field is declared but never invoked anywhere in the class.
- `TrapAssembler.assemble` computes `trapIndexStr` from `record.index()` but never uses it - the real index is the loop
  counter.
