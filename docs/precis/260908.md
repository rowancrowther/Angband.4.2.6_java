# Precis — 2026-09-08

## Key points from the day

### `ObjectIgnore.ignoreBirthInit` documented and covered, giving the ignore subsystem its birth-time reset

`ignoreBirthInit` clears every ignore setting back to its birth-time default - the per-kind flags, the quality bands,
and the per-ego ignore-by-type marks - and is called once from `PlayerBirth` at the same point in birth as the C
original. Nothing downstream depended on documentation or tests for it yet, so today's pass touched no other command,
but it completes yesterday's ignore-flag trio with the one place that actually calls its clearing method. Verification
found the three sweeps and the shared `ITYPE_MAX` sentinel boundary already an exact match for C, so the pass was pure
documentation and testing rather than a fix. A new test suite pins every sweep reaching more than its first entry and
the sentinel slot being left untouched in both the quality-band map and an ego's own ignore-type map, so the birth
screen's ignore reset now has a verified base underneath it.

### `Message.messageAdd` documented and covered, giving the message log a silent write path alongside the announced one

`messageAdd` records a message in the recent-message log without raising an event, standing in as the log-only half of
`messageType` for callers that don't need the boundary to hear about it. The first verification pass caught a real gap:
the method was missing the 2048-entry cap that `messageType` already enforces, so a long enough run would have let the
log grow without bound instead of holding steady like C's `messages->max`. Once the cap was added the port matched
clause-for-clause, so the rest of the pass was documentation and tests rather than a further fix. A new test suite pins
the silent write, the log's own repeat-coalescing, the type-sensitive split, and the cap boundary itself, so the log's
two entry points can no longer drift apart unnoticed.

### `PlayerHistory.clear` documented and covered, giving the history ledger its reset for a fresh game

`clear` empties a player's history ledger back to nothing readable, standing in for C freeing its array and zeroing its
cursor and length. Nothing else in the ledger needed to change since the port already carries C's three fields as one
list, so this pass only adds the missing verification around the one method the ledger's existing test suite had not yet
reached. Verification found the reset already an exact match for C's freed-and-renulled state, so the solution was
documentation and tests rather than a fix. The existing ledger test class now also proves a clear leaves a stale
reference to the old entries untouched, so a future change from list-replacement to in-place clearing would be caught
immediately.

### `Player.initSpellOrder` and
`PlayerMagic.playerSpellsInit` documented and covered, giving the player's spell-tracking lists their birth-time allocation

`playerSpellsInit` sizes a caster's spell-order and spell-flag lists to the class's total spell count and fills the
order list with the "no spell learned yet" sentinel, mirroring C's paired allocation; a non-caster is left untouched.
Verification caught a real gap: `initSpellOrder` cleared and refilled `spellOrder` on every call but never reset
`spellFlags`, so a repeat call would grow the flag list instead of replacing it and let a previously-learned flag
survive a fresh allocation. Adding a matching clear for `spellFlags` brought the two lists back into lockstep. A new
test suite pins the ordinary caster and non-caster paths and, specifically, that a second call replaces rather than
appends to both lists and resets a stale flag, so that invariant can't drift apart again unnoticed.

### `KnownObject.setToH`, `setToD` and
`setToA` documented and covered, giving the combat-bonus knowledge bits their direct setters

The three setters write the to-hit, to-damage and to-armour knowledge bits outright, standing in for the bare field
writes birth uses to hand a character all three combat runes at once, alongside the rune-learning path the class already
carried. Nothing downstream calls them yet, so this pass adds no new behaviour to the rest of the codebase, just
documentation and tests for setters already sitting in the file. Verification found each a plain, unvalidated field
write matching C exactly, so the pass was documentation and testing rather than a fix. A new test class pins each
setter's round-trip to C's birth value of 1 and the three fields' independence from one another, so a future caller can
rely on the same guarantee the rune-learning path already has.

### `GenChunk.getChunkListMax`/
`setChunkListMax` documented and covered, giving the chunk-list bookkeeping counter a verified base

`GenChunk` carries the current count of chunks held in the saved-chunk list, standing in for C's file-scope counter that
the (not yet ported) list-management routines increment and decrement as chunks come and go. The field and its accessors
were already in place but undocumented and untested, so this pass adds no new behaviour to the rest of the codebase,
just documentation and tests for a class already sitting in the package. Verification found the counter's initial value,
its widened type, and both accessors already an exact match for C, so the pass was documentation and testing rather than
a fix. A new test suite pins the default value, a round-trip through both accessors, an overwrite by a second call, and
the top of C's original value range, so the counter has coverage in place ahead of the chunk-list functions that will
eventually build on it.

### `GameState.getSeedFlavour`/
`setSeedFlavour` documented and covered, giving the object-flavour RNG seed a verified accessor pair

The pair stores and returns the seed that gives a game its consistent object-flavour (colour) assignment, standing in
for the bare reads and writes C makes to its file-scope `seed_flavor` global at birth, save and load. Nothing downstream
calls either method yet, so this pass adds no new behaviour to the rest of the codebase, just documentation and tests
for an accessor pair already sitting in the class. Verification found the store and return an exact, unvalidated match
for C's global access with no accessor functions of its own. A new test suite pins the round trip, a second write
replacing the first, the top of C's birth-time `randint0` range, the full unsigned 32-bit range surviving without
truncation, and independence from the turn counter, so the seed has coverage in place ahead of the birth and save/load
code that will eventually set and read it.

### `ObjectKind.setFlavour` documented and covered, giving the per-kind flavour field its own setter

`setFlavour` writes the flavour a kind is disguised behind, standing in for C's bare `kind->flavor = f`
struct field write, which has no dedicated setter and appears inline at every one of its three call sites. Nothing
downstream calls the Java setter yet beyond the birth-time flavour scrub already in
`ObjectUtils`, so this pass adds no new behaviour to the rest of the codebase, just documentation and tests for a setter
already sitting in the class. Verification found the field write an exact, unvalidated match for C's own assignment,
including its double duty of both attaching a real flavour and clearing one back to null. A new test pair pins a flavour
being stored exactly and a later `null`
clearing a previously-attached one, so the setter has coverage in place ahead of the fixed- and random-flavour
assignment code that will eventually call it for real.

### `ObjectUtils.flavourResetFixed` documented and covered, giving the randart flavour scrub a verified base

`flavourResetFixed` clears the resolved sval on every fixed flavour bar the One Ring, so a randarts game doesn't get
predictable flavours for rings and the like carried over from a normal one. The first verification pass caught two real
problems: a stray colon in place of a semicolon at the call site that kept the whole file from compiling, and a skip
condition that spared whichever flavour happened to sit at file index 1 instead of checking for the One Ring itself.
Once both were fixed the method matched C's tval-and-text guard clause for clause, so the rest of the pass was
documentation and tests. A new test suite pins an ordinary fixed flavour being reset, a non-ring flavour sharing the One
Ring's text still being reset, the One Ring itself surviving, and every flavour kind in the registry being walked, so
the birth-time randart flavour scrub now has coverage matching its actual C-derived rule.

### `ObjectUtils.flavourAssignFixed` documented and covered, giving the fixed-flavour binding pass a verified base

`flavourAssignFixed` binds every fixed flavour onto the object kinds sharing its tval and sval, the step that turns a
resolved sval from data-file parsing into an actual flavour on the kind table. Nothing downstream depended on
documentation or tests for it yet, so this pass adds no new behaviour to the rest of the codebase, just documentation
and tests for a method already sitting in the file. Verification found the match test and the unconditional overwrite of
an existing flavour already an exact match for C, so the pass was documentation and testing rather than a fix. A new
test suite pins an ordinary match, both halves of the tval/sval guard, the overwrite of a kind's existing flavour, every
matching kind being bound rather than just the first, the SV_UNKNOWN skip, and multiple flavour groups being walked, so
the fixed-flavour half of birth-time flavour assignment now has coverage matching its actual C-derived rule.

### `Flavour.setText` documented and covered, giving the flavour-text field its own setter

`setText` overwrites a flavour's displayed text, standing in for C's bare `f->text = ...` struct field write, used when
a random scroll flavour is retitled from the `scroll_adj` table once its sval is chosen. Nothing downstream needed
documentation or tests for it yet beyond the one call site already in `ObjectUtils`, so this pass adds no new behaviour
to the rest of the codebase, just documentation and tests for a setter already sitting in the class. Verification found
the field write an exact, unvalidated match for C's own assignment, with no bounds or ordering to diverge on. A new test
suite pins a plain overwrite, a `null` write, and the setter's independence from the flavour's other fields, so the
setter has coverage in place ahead of the random-flavour assignment code that will eventually call it for real.

### `ObjectUtils.flavourAssignRandom` documented and covered, giving the random-flavour binding pass a verified base

`flavourAssignRandom` hands every still-unflavoured kind of a given tval a random flavour drawn from that tval's unbound
pool, the counterpart to `flavourAssignFixed` for the sub-types the data files leave to chance. Nothing downstream calls
it yet - `flavourInit` stops after the fixed-assignment pass - so this pass adds no new behaviour to the rest of the
codebase. Verification caught the outer kind guard drifting through several wrong shapes before it matched C's
tval-and-flavour skip exactly, and the count of flavours on offer needed the same fixed/random split the inner draw
already applied. A new test suite pins both halves of the outer guard, a fixed flavour being excluded from both the
count and the draw, a shared pool being split without repeats across several kinds, and the scroll-only text overwrite,
so the random half of birth-time flavour assignment now has coverage in place for whenever it's wired into
`flavourInit`.

### `Name` documented and covered, giving the parsed name-file section its verified domain form

`Name` pairs one raw section number from `names.txt` with that section's word fragments, standing in for the slot C's
`finish_parse_names` fills directly in `name_sections`, with validation and grouping left downstream to
`RandnameType.fromIndex` and `MiscRegistry.setNames`. Nothing downstream needed documentation or tests for it yet beyond
those two existing call sites, so this pass adds no new behaviour to the rest of the codebase, just documentation and
tests for a class already sitting in the package. Verification found no control flow to diverge on - a bare, unvalidated
constructor and accessor pair matching C's own unchecked struct fill, with the file-order word list also confirmed
functionally equivalent to C's reversed one, since the one consumer counts letter transitions rather than reading
position. A new test suite pins the unvalidated section number at its ordinary, zero and out-of-range values, file-order
preservation, and the word list's by-reference, unmodifiable-view behaviour, so the record has coverage in place ahead
of whatever future call site relies on it.

### `ObjectUtils.flavourInit` documented and covered, wiring the birth-time flavour sequence together

`flavourInit` is the top-level routine birth calls to give every ring, amulet, staff, wand, rod, mushroom, potion and
scroll its flavour for the game: the turn-1 rescrub, the randarts gate, the fixed and random assignment passes, the
scroll-title generator, and the final aware-marking sweep, all in the order C's own `flavor_init` runs them. It's also
the method that finally calls
`flavourAssignRandom`, which had sat unreachable since yesterday's pass, so today's work closes that gap and gives the
random half of flavour assignment a live caller for the first time. Verification caught a real bug in the scroll-title
loop: a candidate word generated to test against the length bound was being kept in the title even when the bound
rejected it, so titles could run past the 15-letter body C's own loop enforces; once the word was only appended on
acceptance, the two matched exactly. A new test suite pins the randarts on/off wiring against `flavourResetFixed`, the
corrected title-length bound and uniqueness, the scroll-flavour text hookup, and the final aware-marking pass's three
cases, so the whole birth-time flavour sequence now has a verified base to build the actual birth call site on.

###
`ObjectUtils.flavourSetAllAware` documented and covered, giving the birth-time aware-marking sweep its own verified base

`flavourSetAllAware` marks every flavoured kind aware in one pass, the routine `flavourInit`'s closing step already
calls and the wizard/spoiler tooling and the "know flavors" birth option call directly. Nothing downstream needed
further wiring since the one real caller inside `flavourInit` was already covered yesterday's own test suite; today's
pass gives the method its own documentation and its own direct coverage instead of only the indirect exercise it got
through that caller. Verification found the empty-slot skip and the flavoured-only guard already an exact match for C,
so the pass was documentation and testing rather than a fix. A new test suite pins a flavoured kind becoming aware, an
unflavoured kind left alone whether it started aware or not, the empty-slot skip taking priority over a bound flavour,
and several kinds walked together, so the method now has a verified base of its own independent of whichever caller
happens to reach it.

### `Player.setIsDead` documented and covered, giving the death flag its own verified setter

`setIsDead` flips the player's death flag, standing in for the bare `p->is_dead = ...` assignment C repeats at every
place death or revival happens rather than routing through one function. Nothing downstream needed documentation or
tests for it yet beyond the existing read of `isDead` in the scalar-state suite, so this pass adds no new behaviour to
the rest of the codebase, just documentation and tests for a setter already sitting in the class. Verification found the
field write an exact, unvalidated match for every one of C's scattered assignments, with no branches or bounds to
diverge on. A new test pins both directions - marking dead and the cheat-death revive back to alive - so the one Java
choke point for the flag now has coverage matching C's several call sites.

### `GameState.setCharacterGenerated` and
`PlayerUpkeep.setPlaying` documented and covered, giving the two birth-completion flags their own verified setters

The pair flip the flags that mark a character as existing and a game as actually in progress, standing in for the bare
`character_generated = ...` and `upkeep->playing = ...` assignments C repeats at every birth, save-load, death and quit
call site rather than routing through one function each. Nothing downstream needed documentation or tests for either
beyond the existing reads already covered in the class-scalar and turn-state suites, so this pass adds no new behaviour
to the rest of the codebase, just documentation and tests for setters already sitting in their classes. Verification
found both field writes an exact, unvalidated match for every one of C's scattered assignments, with no branches or
bounds to diverge on. A new test class for `setCharacterGenerated` and a new case in the existing upkeep suite for
`setPlaying` pin a plain round trip, a second write replacing the first, and each flag's independence from its
neighbouring fields, so the two Java choke points now have coverage matching C's several call sites apiece.

### `ObjectRandart.artifactSetDataNew` documented and covered, giving randart generation its C-derived starting state

`artifactSetDataNew` builds the working `ArtifactSetData` a randart generation run accumulates its learned probabilities
into, standing in for C's `mem_zalloc`-then-hand-set-fields construction of the same struct. The constructor it wraps
sits underneath `doRandart`, the not-yet-ported entry point for the whole random-artifact generator, so this is the
first piece of that subsystem to get a verified base. The first verification pass caught a real gap: C's arrays read
zero at any index the moment they're allocated, but the port's maps started out empty, so a lookup before anything had
written to them would return `null` instead of the `0` the rest of the algorithm expects; pre-populating every map's
full key domain at construction closed that gap. A new test suite pins the six hand-set start and increment values,
every counter and map starting at zero across its whole domain (the tval, artifact-index and per-artifact keyed maps
alike), an unloaded artifact registry failing loudly rather than silently, and two calls never sharing state, so the
rest of `do_randart`'s port has a verified foundation to build design_artifact and its neighbours on.

###
`PlayerBirth.doCmdAcceptCharacter` documented and covered, giving the final step of character creation a verified base

`doCmdAcceptCharacter` is the birth screen's closing command: it rolls hit points, opens the message log, embodies and
outfits the player, initialises spells and known runes, restores the artifact table, seeds flavours, then flips the game
to "playing" and fires the leave-birth event, tying together most of the subsystems birth has built up over the last
several days' passes. Verification found two real gaps against C: the unconditional randart-file archive call ahead of
the artifact reload was missing entirely, and the cached roller-history clear at the very end of the function was
absent, so both were added before documentation began. The new test suite runs the method for real against the shipped
race, artifact and flavour data rather than a hand-built fixture, which is what turned up two further pre-existing gaps
one level down - a missing default for several unset dice fields on object kinds, and a missing null guard on one field
`ItemObject.copy()` never checks - plus two subsystems nothing yet wires into the game's own start-up (`player_init`'s
object-knowledge sizing, and the rune list). None of those four are this method's own fault, so the suite works around
them rather than fixing them, and giving the player class no starting items keeps the two item-construction gaps out of
reach until they're fixed for real; the fixes themselves remain open work for whoever next touches the item-construction
or bootstrap code.

---

## Things not to drop

- ignoreLevel exists in both ObjectInfo and ObjectIgnore - need to consolidate into one
